class Collisions {

    // Listen to update event by drawable
    // Iterate through all draw = true
    // Compare to see if any have collided
     // data & object - collidable with object coverage (position + size)
     // obj & obj - coverage and coverage
    // Announce colliding obj entity ids if true
}